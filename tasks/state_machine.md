# state_machine — Implement per-chat state machine for user flows (idle → new_duel → ... → confirming)

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 PDUEL

Implement per-chat state machine for user flows (idle → new_duel → ... → confirming)